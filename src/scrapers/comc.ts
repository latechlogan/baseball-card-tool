import { COMCListing } from '../types.js';

/**
 * Scrape COMC (Check Out My Cards) listings for a given player.
 * Returns active ask prices, parallels, and print runs available for purchase.
 */
// TODO: Implement in Chunk 3
export async function scrapeComc(_playerName: string): Promise<COMCListing[]> {
  throw new Error('Not implemented');
}
