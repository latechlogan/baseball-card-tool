import { EbaySaleRecord } from '../types.js';

/**
 * Fetch PSA population report and recent auction results for a given player's cards.
 * Returns sale records enriched with PSA grade/condition data.
 */
// TODO: Implement in Chunk 3
export async function scrapePsa(_playerName: string): Promise<EbaySaleRecord[]> {
  throw new Error('Not implemented');
}
